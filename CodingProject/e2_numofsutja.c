#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main(void)
{
	int a, b, c, i, k;

	scanf("%d\n%d\n%d\n", &a, &b, &c);

	k = a * b * c;

	for (i = 0; i < ; i++)
	{

	}

	return 0;
}