#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main(void)
{
	int n, i, x, y, a, b, p, q;

	scanf("%d", &n);
	for (i = 0; i < n; i++)
	{
		scanf("%d %d", &x, &y);
		
		x = p;
		y = q;

		a=x + 10;
		b = y + 10;
		
		if (x > p)
		{
			(p+10-x)*
		}
	
	}



	return 0;
}